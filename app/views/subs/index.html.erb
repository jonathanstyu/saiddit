<h1>Welcome to Saiddit!</h1>

<p>A Reddit ripoff for the ages! Log in if you want to create subs.</p>

<table>
  <tr>
    <th>Category</th>
    <th>Moderator</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @subs.each do |sub| %>
  <tr>
    <td><%= sub.category %></td>
    <td><%= User.find_by_id(sub.moderator_id).username %></td>
    <td><%= link_to 'Show', sub %></td>
    <td><%= link_to 'Edit', edit_sub_path(sub) %></td>
    <td><%= button_to 'Destroy', sub_path(sub), method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sub', new_sub_path %>
