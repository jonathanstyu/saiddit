<p id="notice"><%= notice %></p>

<h3>Category: <%= @sub.category.capitalize %></h3>

<b>Moderator:</b> <%= User.find_by_id(@sub.moderator_id).username.capitalize %>

<h3>Links for Sub</h3>
<%= render @sub.links %>

<%= link_to 'Edit', edit_sub_path(@sub) %> |
<%= link_to 'Back', subs_path %>
